# ============================================================
# Caddy 配置 · 科技圈发布会雷达
#
# 【模式一：备案前 · IP 访问】（默认启用）
#   备案通过前域名不能解析到服务器，先用 http://<服务器IP> 测试。
#
# 【模式二：备案后 · 域名 + 自动 HTTPS】
#   备案通过、域名 A 记录解析到本服务器后：
#   1. 注释掉下面的 :80 块
#   2. 取消 nextlaunch.cn 块的注释
#   3. systemctl reload caddy（Caddy 会自动申请并续期 Let's Encrypt 证书）
# ============================================================

# ---- 模式一：备案前 IP 模式 ----
:80 {
	reverse_proxy 127.0.0.1:8787
	encode gzip
}

# ---- 模式二：备案后域名模式（备案通过后启用） ----
# nextlaunch.cn {
# 	reverse_proxy 127.0.0.1:8787
# 	encode gzip
# }
#
# www.nextlaunch.cn {
# 	redir https://nextlaunch.cn{uri} permanent
# }
